# BodyLocaliser dependencies
#
# Install with:  pip install -r requirements.txt
#
# IMPORTANT: PsychoPy requires Python >=3.10, <3.13 (Requires-Python of the release
# the launchers install). Check it if you pin a different PsychoPy version.
# The launcher scripts handle this automatically.
psychopy

# PyInstaller is only needed if you want to build a standalone executable.
# Uncomment the line below if you plan to build bundles.
# pyinstaller>=6.0
